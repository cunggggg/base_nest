// import * as numeral from 'numeral';

export class NumberHelper {
  /**
   * convert number to abbreviate number string
   * @param n: number
   */
  public static toAbbreviateNumber(n: number): string {
    return '';
    // return numeral(n).format('0.0a');
  }
}
